{
  "knowledge_engine_endpoint": "http://tke-runtime:8280/rest",
  "knowledge_base": {
    "id": "https://example.org/a-sql-knowledge-base",
    "name": "Some SQL knowledge base",
    "description": "This is just an example."
  },

  // Connection details for the SQL database
  "sql_host": "mariadb",
  "sql_port": 3306,
  "sql_database": "treedb",
  "sql_user": "user",
  "sql_password": "password",

  "knowledge_interactions": [
    // Knowledge interaction that discloses all trees
    {
      "type": "answer",
      "column_prefixes": {
        "tree": "http://example.org/trees/"
      },
      "vars": ["tree", "height", "name"],
      "pattern": "?tree <https://example.org/hasHeight> ?height . ?tree <https://example.org/hasName> ?name .",
      "sql_query": "SELECT id AS tree, height, name FROM trees"
    },
    // Knowledge interaction that WRITES trees to the SQL database when they are
    // POSTed to the interoperability layer
    {
      "type": "react",
      "column_prefixes": {
        "tree": "http://example.org/trees/"
      },
      "vars": ["tree", "height", "name"],
      "argument_pattern": "?tree <https://example.org/hasHeight> ?height . ?tree <https://example.org/hasName> ?name .",
      "result_pattern": null,
      "sql_query": [
        {
          "statement": "INSERT INTO trees (id, height, name) VALUES (%s, %s, %s)",
          "bindings": ["tree", "height", "name"]
        }
      ]
    }
  ]
}